server:
  port: 9091

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/payment-service
    username: root
    password: Root
   
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
